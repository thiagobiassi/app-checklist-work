<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.slfleet.checklistapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.slfleet.checklistapp.activity.SendTermActivity" />
        <activity android:name="com.slfleet.checklistapp.activity.ChecklistActivity" />
        <activity android:name="com.slfleet.checklistapp.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.slfleet.checklistapp.activity.MainActivity"
            android:label="Tarefa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.slfleet.checklistapp.activity.AddNewTask" />
    </application>

</manifest>